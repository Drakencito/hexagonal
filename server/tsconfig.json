{
  "compilerOptions": {
    "target": "ESNext",                      /* versión de ECMAScript objetivo */
    "module": "commonjs",                    /* sistema de módulos */
    "strict": true,                          /* habilitar todas las comprobaciones estrictas de TypeScript */
    "esModuleInterop": true,                 /* interoperabilidad entre espacios de nombres de módulos */
    "skipLibCheck": true,                    /* omitir la comprobación de bibliotecas de definiciones de TypeScript */
    "forceConsistentCasingInFileNames": true,/* forzar consistencia de mayúsculas y minúsculas en los nombres de archivo */
    "outDir": "./dist",                      /* directorio de salida para los archivos compilados */
    "rootDir": "./src",                      /* directorio raíz de entrada de los archivos TypeScript */
    "resolveJsonModule": true,               /* habilitar la resolución de módulos JSON */
    "baseUrl": "./",                         /* base URL para las rutas relativas en el proyecto */
    "paths": {
      "*": [
        "node_modules/*"
      ],
      "typeRoots": [
      "./node_modules/@types",
      "./usuarios/infrastructure/types"
    ]
    }
  },
  "include": [
    "src/server.ts"                           /* archivos que deben ser incluidos en la compilación */
  ],
  "exclude": [
    "node_modules"                          /* archivos que deben ser excluidos de la compilación */
  ]
}